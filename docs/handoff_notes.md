# ElegenCashBook — Handoff Notes

**Date:** 2026-07-12
**Branch:** `feature/p6-rbac` (off `master`) — P6+P7 work-in-progress branch, uses its own Supabase project (`cashbook.permissions`, id `khyjmbsgekldfhflcbfs`), not the hosted prod project. `master` still 1 commit ahead of `origin/master` (`89d66ef` unpushed).
**Governing docs:** [constitution.md](constitution.md) (rules), [implementation_specs.md](implementation_specs.md) v1.2.0 (spec), [implementation_plan.md](implementation_plan.md) (phased plan, proof-gated)

---

## 1. Current state

Offline-first Android cashbook. Phases P0–P5 done, proof gates passed. P6 (RBAC backend) and P7 (sharing UI + Realtime) both done on the `feature/p6-rbac` branch against the `cashbook.permissions` dev project — not yet merged to master or re-pointed at the hosted prod Supabase project. **P7's two-account/two-device proof gate has not been run** — needs a real second account and either two devices or two emulators, which only the user can do.

| Phase | Status |
|---|---|
| P0 Baseline | done, gate passed |
| P1 Money core (`Long` paisa) | done, gate passed |
| P2 Room + MVVM | done, gate passed |
| P3 Supabase auth + identity scoping | code done; **on-device proof gate incomplete** (device keeps dropping off wireless adb) — server-side (RLS, curl matrix) verified, on-device tap-through not yet re-confirmed by user |
| Post-P3 hardening | code-review pass (7 findings) fixed + verified; 3 user-reported bugs (backdated-entry grouping, book-details 3-dot menu, stale "Updated" label) fixed today |
| P4 Push sync | **done.** Outbox table + Migration(1,2), atomic entity+outbox writes (`OutboxWriter`), `SyncPushWorker` (PostgREST upsert, backoff, dead-letter), `WorkManagerSyncScheduler`, `RemotePush`. Server tables/RLS in `supabase/migrations/20260704000002_p4_sync_tables.sql` (applied). Migration(2,3) backfills pre-outbox local data; `SyncQueueDao.pending()` tiers BUSINESS→BOOK→TRANSACTION (not just insertion id) so backfilled parents always push before already-queued children. Proof gate: airplane-mode round trip PASS (34→52 transactions synced), kill-mid-sync PASS (0 pending post-restart, 52/52 distinct ids, zero dead-letters), cross-user RLS isolation PASS (curl+2 real JWTs: spoofed insert 403, cross-user update 0 rows, cross-user delete 403 — DELETE not even granted to `authenticated`, soft-delete-only by design). Supabase-down resilience not tested (hosted project, not local Docker — would need pausing via dashboard/MCP) and P0–P3 regression not formally re-run — accepted as-is per explicit call, not blocking. |
| P5 Pull sync + conflicts | **done, proof gate passed 2026-07-12.** `RemotePull` (delta select `updated_at > lastPulledAt`, cursor per entity type in `AppPreferences`), `ConflictResolver` (pure LWW: higher `updatedAt` wins, `deviceId` tiebreak on tie — 7 unit tests), `SyncPullWorker` (same Hilt-`EntryPoint`-on-`CoroutineWorker` pattern as `SyncPushWorker`), `CleanupWorker` (purges tombstones >30 days old, idle+charging, daily). Pulled one-time on login/app-start plus a 15-min periodic backstop (WorkManager's minimum interval — Realtime foreground push is P7, deliberately not built yet). Fresh-install hydration needs no special code: cursor defaults to 0, so the first pull is naturally a full pull. No new Room migration needed (no new columns/tables). Two-device proof gate verified: device A add → appears on B, concurrent edit → same LWW winner both sides, delete propagates, uninstall/reinstall/login → full restore, P0–P4 regression still green. |
| P6 RBAC backend + permission core | **done, proof gate passed 2026-07-12, on `feature/p6-rbac` branch only.** Server: `business_members`/`book_grants`/`audit_log` tables, `business_role()`/`effective_perms()` SQL functions (mirror spec §8.3's `effective(user,book)` exactly), full RLS replacing P4's owner-only policies, `enforce_business_perms`/`enforce_book_perms`/`enforce_tx_perms` BEFORE UPDATE triggers (RLS can't diff OLD vs NEW to split e.g. BOOK_EDIT from BOOK_DELETE, so a trigger does the fine-grained check on top of RLS's coarse gate), `protect_last_owner` trigger, 7 SECURITY DEFINER RPCs (`lookup_user`, `invite_to_business`, `share_book`, `update_member_role`, `set_book_grant`, `revoke_member`, `revoke_book_grant`) — all in `supabase/migrations/20260712000001_p6_rbac.sql`, applied to the `cashbook.permissions` dev project (id `khyjmbsgekldfhflcbfs`), not yet to hosted prod. Client: `BusinessMemberEntity`/`BookGrantEntity` Room mirrors (pull-only, no outbox — writes are RPC-only), `MIGRATION_4_5`, `core/permission/Permission.kt` (pure-Kotlin `PermissionResolver.effective()` mirroring the SQL function, 13 table-driven tests), `PermissionRepository`/`GetEffectivePermissions` use case, wired into `BookDetailsViewModel.state.permissions` (no visible UI change — owner's set is always everything, sharing UI is P7). Proof gate: 22/22 pgTAP assertions green (`supabase/tests/p6_rbac_pgtap.sql` — non-member denied, VIEWER write denied, DENY hides book, scoped-admin allow-list both ways, business_members/book_grants/audit_log not client-writable, last-owner rejected, TX_EDIT-without-TX_DELETE override split, personal-book share_book flow, lookup_user exact-match), cross-tenant `businesses` table read confirmed 0 rows for a non-member, full Android regression 122/122 unit tests green (was 109 before P6 — 13 new `PermissionResolverTest` cases). No on-device manual proof needed/run — P6 explicitly has no UI change to tap through. |
| P7 Sharing UI + collaboration | **backend/client code done 2026-07-12, on `feature/p6-rbac` branch only; two-device proof gate NOT run (needs the user). Real production-blocking bug found + fixed during on-device testing — see below.** Server: `list_business_members`/`list_book_grants` SECURITY DEFINER RPCs resolving email/display_name (users table isn't otherwise selectable for others) — `supabase/migrations/20260712000002_p7_list_rpcs.sql`; `books`/`transactions` added to the `supabase_realtime` publication — `..._p7_enable_realtime.sql`. Client: `SharingRepository`/`SharingRepositoryImpl` (network-only, no local path — sharing requires online by design, RPC failures map to friendly messages: USER_NOT_REGISTERED/LAST_OWNER/FORBIDDEN/etc via `PostgrestRestException.message` pattern-matching, same style as `AuthRepositoryImpl`), use cases (`InviteToBusiness`, `ShareBook`, `SetBookGrant`, `UpdateMemberRole`, `RevokeMember`, `RevokeBookGrant`, `ListBusinessMembers`, `ListBookGrants`, `GetMyBusinessRole`). UI: `MembersActivity` (business member list + invite sheet, role change/revoke via overflow menu, gated on OWNER/ADMIN via `GetMyBusinessRole`) reachable from Business Settings' new "Members" row; `BookAccessActivity` (per-book grant list + share sheet with Viewer/Editor perms, gated on `MEMBER_MANAGE` via `GetEffectivePermissions`) reachable from the book 3-dot menu's "Add Members" row (previously a "coming soon" placeholder) and `BookDetailsActivity`'s toolbar person-add icon (previously unwired). `RealtimeSync` subscribes to the active business's `books`/`transactions` postgres_changes while `MainActivity` is foreground (starts/stops on business switch and app background), merging via the same `ConflictResolver` + JsonObject→Entity mappers `RemotePull` uses (now `internal` instead of `private` for reuse) — backgrounded devices still get changes via the existing pull worker, Realtime is a latency improvement only. Also fixed a real bug found in passing: `BusinessDao.observeWithBookCount` filtered `WHERE ownerUid = :uid` only, so a business shared with you (not owned by you) would never have appeared in the business list/switcher at all — now also matches an ACTIVE `business_members` row. Full Android regression 122/122 unit tests green, `assembleDebug` clean.

**Real two-account testing started 2026-07-12 — found and fixed 4 more real bugs beyond the two above:**
1. **`BookAccessActivity`'s share button invisible** — same root cause as the Members bug (#2 above), just in the book-level screen: `canManage` needed `business_id` passed through as an intent extra (it wasn't) plus a network-derived check, not the local mirror. Fixed.
2. **No repository-layer permission enforcement at all (the big one).** `BookRepositoryImpl`/`TransactionRepositoryImpl` predate RBAC and only ever checked raw `ownerUid == me` (`ownedBook()`) — completely unaware of shared ADMIN/VIEWER roles. Consequences actually hit on-device: a VIEWER's rename silently no-op'd with zero error shown (`ownedBook()` returned null → `?: return`); **`duplicate()` crashed the app** (`?: error("Book not found")`, uncaught — same VIEWER tapped Duplicate); a VIEWER could add/edit/delete entries locally with no gate at all (`TransactionRepositoryImpl` had never had any capability check, even pre-RBAC). Fixed by replacing `ownedBook()` with real capability checks via `PermissionResolver` (`BOOK_EDIT`/`BOOK_DELETE`/`BOOK_ADD` for books, `TX_ADD`/`TX_EDIT`/`TX_DELETE` for transactions), throwing a new `PermissionDeniedException` instead of silently no-op'ing or crashing. This also **fixed a real functionality gap**, not just a security hole: a legitimate shared ADMIN previously couldn't rename/delete a book they didn't personally create either (raw ownership check blocked them too) — now they correctly can.
3. **Every book/entry mutation was fire-and-forget with no error handling** (`MainViewModel`, `BookDetailsViewModel`, `EntryDetailsViewModel`) — an uncaught `PermissionDeniedException` from fix #2 would otherwise still crash the app. Added `errorMessage` to each UiState + a `runGuarded` wrapper, shown as a Toast in the Activities.
4. **`BookDetailsActivity` never actually gated any buttons on `permissions`** (P6 built the plumbing, P7 was supposed to wire it, hadn't been done) — Cash In/Out buttons and the 3-dot menu's rename/duplicate/move/delete/add-members items are now enabled/visible only per the real effective permission set.
5. **Business switcher always showed "Owner"** regardless of actual role (`BusinessItem.roleLabel` was hardcoded) — B saw "Owner" while actually invited as Viewer. Fixed via a new local-mirror lookup (`GetMyBusinessRole`, display-only, blank while the mirror hasn't caught up rather than guessing).

Also discovered and documented a recurring bug *class* this session: any "can I do X" check that depends on the local Room mirror of server-granted membership/role will intermittently be wrong right after something is just created/shared, because that mirror only populates after a pull cycle. Hit **three separate times** (Members invite button, BookAccess share button, and — most severely — `PermissionRepositoryImpl.computeEffective()` itself, which would have blocked an OWNER from managing their own just-created book). All three fixed by falling back to a network call or a raw local ownership check instead of trusting the mirror's absence as "no access." See `project_p7_upsert_rls_deadlock` memory for the full pattern.

**Still not verified**: real two-account invite/share/revoke flow end-to-end without hitting a new bug, Realtime live-update latency, and — now that repository-layer gating exists — whether a VIEWER/scoped-ADMIN's blocked action correctly shows the new error toast on a real device (only unit-tested so far). See §5 for exact manual steps.

**Bug found + fixed during real on-device testing (2026-07-12):** every push of a brand-new business/book/transaction was silently rejected by the server with a 403 — nothing the app created ever reached Supabase, discovered when Members showed "you don't have permission" for the business's own owner. Root cause: `postgrest-kt`'s `upsert()` always compiles to `INSERT ... ON CONFLICT DO UPDATE`; Postgres requires the row to satisfy the **SELECT** policy (to check whether a conflicting row exists) even when there's no actual conflict (a pure first-time insert). The P6 `sel_biz`/`sel_book`/`sel_tx`/`upd_biz`/`upd_book`/`upd_tx` policies all gated on `business_role(id)`/`effective_perms(id)`, which require the row — and for businesses, the owner's own `business_members` row — to already exist: a chicken-and-egg deadlock that blocked every fresh insert from ever landing. This wasn't caught by the P6 pgTAP suite because that suite only ever seeded fixture rows directly (bypassing RLS as `postgres`), never tested a real client-shaped `INSERT ... ON CONFLICT DO UPDATE` as an ordinary authenticated user creating something for the first time. Fixed in `supabase/migrations/20260712000004_p7_fix_upsert_rls_deadlock.sql` — ORs in the row's own ownership/creator column (`owner_uid = auth.uid()` / `created_by_uid = auth.uid()`) as a fallback on all six policies; fine-grained capability enforcement is unaffected since that lives in the `enforce_*_perms` triggers, which only fire on a genuine UPDATE (real conflict), never on this insert-only bootstrap path. Re-verified: pgTAP suite now 23/23 (added a regression case for this exact scenario), cross-tenant isolation unchanged. **Server-side only — no client rebuild needed**, existing pending local businesses should push successfully once sync next runs. |

Commits on top of P2: `956473a` (P3), `16129d5` (review fixes), `89d66ef` (today's 3 bugs). Local test suite: 46 tests, 0 failures. `assembleDebug` clean. `installDebug` currently fails — **no device connected**; reconnect before trusting anything beyond compile+test.

---

## 2. Architecture

Clean Architecture + MVVM + Repository, enforced by grep-checkable rules (constitution/spec §4):

```
UI (Activity)  →  ViewModel (StateFlow<UiState> / onEvent(UiEvent))  →  UseCase  →  Repository (interface, domain/)  →  RepositoryImpl (Room-only, data/)  →  Room
```

- **domain/** is pure Kotlin — no Android, no Room, no Supabase imports. This was tested mid-session: a code-review suggestion to expose `AuthRepository.handleDeepLink(intent)` was rejected on implementation because `Intent` is an Android type; instead a `data`-layer-only `AuthDeepLinkHandler` was added. **If a fix needs an Android/SDK type in a repository method, the fix belongs in a new `data/` class, not the domain interface.**
- **UI never imports Room/DAO/Supabase directly.** Existing XML layouts are reused as dumb shells (`activity_main`, `activity_book_details`, `activity_add_business`, bottom sheets, `popup_book_menu`) — UI is swappable later (Compose migration is a deferred, UI-layer-only swap).
- Every entity carries a `SyncEnvelope` (`version`, `updatedAt`, `deviceId`, `deletedAt`, `syncState`) — this is P4's outbox foundation, already stamped on every write since P2.
- Money is always `Long` paisa via `@JvmInline value class Money` — zero `Double`/`Float` in `app/src/main/java` (grep-verified at P1 gate, still true).

---

## 3. Key decisions and why

- **Email+password auth, not magic link.** Magic link needs deep-link plumbing for zero benefit here; email+password was simpler and got its own deep-link handling anyway (email confirmation flow).
- **Hosted Supabase, not local Docker.** Switched mid-P3 so the device doesn't need to be on the same Wi-Fi as the dev machine. `local.properties` holds hosted URL + publishable key (gitignored). DDL only applied via dashboard SQL Editor — **auto-mode blocks DDL against a live DB by design, do not bypass this.**
- **Identity-scoping model** (`IdentityManager`): guest sentinel `"guest"` vs real uid. Guest-created data is claimed (reassigned) on login; on logout the active id flips back to guest, hiding — not deleting — the user's rows (retain-but-hidden). This was a direct fix for a real data-leak bug (all local rows shared one bucket, any account saw everyone's books). Legacy `"local"` owner rows are migrated to `"guest"` once at startup.
- **`ActiveIdentity` interface seam.** `IdentityManager` was hard to fake in tests (needs a full auth graph). Extracted a narrow `ActiveIdentity` interface (`activeUid: StateFlow<String>`, `current(): String`) so repo tests can inject a `FakeIdentity` without touching DI. Minimal seam, not a general abstraction — don't grow it beyond what a repo actually needs.
- **`db.withTransaction` for multi-row writes.** `BookRepositoryImpl.duplicate()` copies a book + all its entries; wrapped in a Room transaction so a crash mid-copy can't leave a half-duplicated book. Proven with a real regression test: the fix was temporarily reverted, the test was confirmed to fail specifically (not something else), then restored.
- **Ownership guard on every book mutation.** `ownedBook(bookId)` checks `ownerUid == identity.current()` before rename/delete/restore/move/duplicate — belt-and-suspenders alongside the UI's own scoping, cheap enough to always do.
- **`lastEntryAt` = real wall-clock time, not the user-picked entry date.** The book-list "Updated X" label was reading `MAX(t.createdAt)` (the date/time the user picked in the entry-date picker), so backdating an entry could make the whole book look artificially old or fresh. Fixed to `MAX(b.updatedAt, MAX(t.updatedAt))` — the sync envelope's actual write time, folding in the book's own last-touch so a rename/move updates it even with zero entries.
- **Per-date entry-list headers instead of one static header.** Same root cause class as above: a single `tv_date_header` always showed the newest entry's date, so backdated entries visually appeared "under today." Replaced with dynamic per-group headers built in `refreshEntryList`, keyed off `entry.dateText` changing between consecutive (already date-sorted) entries.
- **No Undo snackbar for book-delete from inside `BookDetailsActivity`.** Main's book-list delete has Undo because the screen stays open; book-details has to `finish()` once its own book is gone, so Undo would show on a screen that no longer exists. Simplified to toast + finish. Flagged as a conscious simplification, not an oversight — add if a cross-screen undo is ever wanted.
- **Guest-mode choice is in-memory, not persisted.** `AppPreferences.guestModeChosen` was wrongly a DataStore-persisted flag, so "continue as guest" survived forever, including across cold app kills — the spec wants a fresh login prompt on every cold launch until the user chooses again. Now `MutableStateFlow(false)`, reset every process start.

---

## 4. Coding conventions established

- **Repository pattern:** interface in `domain/repository/Repositories.kt`, impl in `data/repository/RepositoryImpls.kt`, Hilt `@Binds` wiring in `data/di/AppModules.kt`. New repo methods always go interface → impl → binding, in that order.
- **Use cases:** one class per operation, `operator fun invoke(...)`, constructor-injects only repository interfaces (never DAOs directly). Validation (`require(...)`) lives in the use case, not the repository or UI.
- **ViewModel contract:** `sealed interface XUiEvent`, `data class XUiState`, single `val state: StateFlow<XUiState>`, single `fun onEvent(event: XUiEvent)`. No other public surface.
- **Soft delete everywhere:** `deletedAt` tombstone + `restore()`, never a hard `DELETE`. Every soft-delete path pairs with a restore path and (in UI) a confirm dialog.
- **Sync envelope bump on every write:** `newEnvelope(...)` for creates, `.bumped(...)` for updates — always stamps `version++`, `updatedAt = now()`, `deviceId`, `syncState = PENDING`. Don't hand-roll a write that skips this; P4's outbox depends on it being universal.
- **Test split:** Robolectric + JUnit4 vintage engine for anything touching Room/Android (`*Test.kt` under `data/`), JUnit5 (Jupiter) + MockK/Turbine for pure-Kotlin domain/usecase tests. Don't mix — check an existing sibling test file for which runner a new test belongs under.
- **Testability seams via narrow interfaces, not broad ones.** `ActiveIdentity` is the model: extract exactly the surface a test needs, nothing speculative.
- **Regression-proof risky fixes.** For anything non-obvious (the transaction-atomicity fix), temporarily break the fix, confirm the new test catches it specifically, then restore. Don't just trust that a test "looks right."
- **Never log** balances, uids, emails, phones, or tokens (constitution security rule) — `Logger` facade in `core/logging`, debug-gated by `BuildConfig.DEBUG`.
- **Never write DB passwords to any file; never ship `service_role`/`SECRET_KEY` client-side.**
- Comments only for non-obvious *why* (a hidden constraint, a workaround, a subtle invariant) — not restating what the code does. This session's diffs follow that: e.g. the `ownedBook()` and `lastEntryAt` query comments explain the reason, not the mechanics.

---

## 5. Concrete next steps

**P7 two-account/two-device proof gate** (per implementation_plan.md's Proof Gate P7 — the only thing standing between "code done" and "phase done"; needs two real accounts, requires this branch's `local.properties` pointed at `cashbook.permissions`, not hosted prod):

1. Register account A and account B (two devices/emulators, or one device + one emulator).
2. As A: create a business, open Business Settings → Members → invite B by email as ADMIN. Confirm B's device shows the business in its switcher after next pull/login (this was the shared-business-visibility bug fixed today — confirm it actually shows).
3. As B: add an entry to one of A's books. Confirm it appears on A's device — with A's app foregrounded on that business, this should arrive near-instantly via Realtime; confirm it also arrives via the periodic pull worker if A is backgrounded first.
4. As A: on a specific book, open the 3-dot menu → "Add Members" (or the toolbar person-add icon in Book Details) → share with a VIEWER-only permission set. Confirm that account sees the book read-only (no add/edit buttons meaningfully enabled — note P7's UI gating is coarse: `MembersActivity`/`BookAccessActivity` hide invite/manage controls for non-managers, but the main entry-list screens don't yet hide CASH IN/OUT buttons per-permission — flag this if it matters for launch).
5. Invite an unregistered email — confirm the friendly "This person must install and register first" message appears (not a raw Postgres error).
6. As A (sole owner): try to remove yourself from Members — confirm it's rejected with the last-owner message.
7. Revoke B's access — confirm B's next pull/login no longer shows that business/book.
8. Full regression: confirm P0–P6 single-user offline flows are unaffected (create/edit/delete books and entries while guest, login, sync) — nothing in P7 should have touched that path, but it's the standing rule for every phase gate.

**Before any of this**: this branch's Supabase project (`cashbook.permissions`) is empty/fresh — register real test accounts on it, don't expect existing prod data.
